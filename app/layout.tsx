import type { Metadata } from "next";
import "./globals.css";
import Header from "./components/Header";
import Footer from "./components/Footer";

export const metadata: Metadata = {
  title: "Khushi Finance | Trusted Loan Consultancy Partner in India",
  description: "Khushi Finance is India's leading loan consultancy company. Get the lowest interest rates on Home, Business, Personal Loans and Loans Against Property with zero hidden charges.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
